// UC1: Check if employee is present or absent

const isEmployeePresent = Math.floor(Math.random() * 2); // 0 or 1

if (isEmployeePresent) {
    console.log("Employee is present.");
} 
else {
    console.log("Employee is absent.");
}